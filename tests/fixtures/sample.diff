diff --git a/src/auth.ts b/src/auth.ts
index 1111111..2222222 100644
--- a/src/auth.ts
+++ b/src/auth.ts
@@ -10,6 +10,11 @@ export function login(user: string, pass: string) {
   const q = "SELECT * FROM users WHERE name = '" + user + "'";
   return db.query(q);
 }
+
+export function isAdmin(req: any) {
+  return req.query.admin === "true";
+}
+
+const API_KEY = "sk-live-1234567890abcdef";
+export default API_KEY;
diff --git a/src/util.ts b/src/util.ts
index 3333333..4444444 100644
--- a/src/util.ts
+++ b/src/util.ts
@@ -1,3 +1,4 @@
 export function add(a: number, b: number) {
-  return a + b;
+  return a + b + 0;
 }
